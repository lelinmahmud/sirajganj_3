<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.smarifrahman.sirajganj_3">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.area.MyAreaActivity"></activity>
        <activity android:name=".ui.communication.CommunicationActivity" />
        <activity android:name=".ui.number.EmergencyNumberActivity" />
        <activity android:name=".ui.job.JobsActivity" />
        <activity android:name=".ui.bazar.BazarActivity" />
        <activity android:name=".ui.news.NewsDetailsActivity" />
        <activity android:name=".ui.opinion.OpinionActivity" />
        <activity android:name=".ui.news.NewsActivity" />
        <activity android:name=".ui.main.MainActivity" />
        <activity android:name=".ui.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>